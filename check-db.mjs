import { PrismaClient } from '@prisma/client'; import { PrismaMariaDb } from '@prisma/adapter-mariadb'; const adapter = new PrismaMariaDb('mariadb://root:123700@localhost:3306/ljma_accounting'); const prisma = new PrismaClient({ adapter }); async function main() { console.log('Sales Count:', await prisma.posSale.count()); console.log('Sync Logs:', await prisma.posSyncLog.findMany()); } main().catch(console.error).finally(()=>prisma.$disconnect());
